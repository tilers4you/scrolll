/node_modules
/.next
/out
/build
*.log
.DS_Store
.env*
!.env.example

# Frame sequences live in the repo for this site (~68MB).
# If you swap to a larger clip, consider Git LFS or a CDN.
/public/frames-mobile/*

# tsc incremental cache
*.tsbuildinfo
